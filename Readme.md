TestIT Report
=============

Сервис выгрузки тест-кейсов из TestIT по шаблону


Installation
============

create file .env
```shell script
TOKEN=<TOKEN_FROM_TEST_IT>
URL=<TEST_IT_URL>
```

start container:
```shell script
docker-compose up -d
```

open:
```shell script
http://localhost:5678
```